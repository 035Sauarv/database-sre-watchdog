version: 0.0
os: linux
files:
  - source: /backend
    destination: /home/ubuntu/sre-watchdog/backend
  - source: /frontend/dist
    destination: /var/www/html/sre-watchdog
hooks:
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root